# Basic Flask task board

This is the smallest complete Teloce example. It has a Flask HTML shell, a `.vel` entry component, an imported child component, and a repeatable build.

```bash
python -m pip install -r requirements.txt
python app.py
```

`app.py` builds the `.vel` files before starting Flask. Open <http://127.0.0.1:5001>.
