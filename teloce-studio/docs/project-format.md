# Project format

Each Studio project will contain a versioned `teloce-studio.json` file describing pages, components, props, events, styles, routes, APIs, and backend resources. The format must be migratable and must never require the editor to run the generated application.
